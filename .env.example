# Server
PORT=3000
NODE_ENV=development
SESSION_SECRET=change-me-to-a-long-random-string

# Database
DATABASE_URL=postgres://user:password@localhost:5432/bunca

# Admin bootstrap (used by `npm run seed`)
ADMIN_EMAIL=admin@bunca.local
ADMIN_PASSWORD=ChangeMe123!

# Render note:
# On Render, set DATABASE_URL, SESSION_SECRET, ADMIN_EMAIL, ADMIN_PASSWORD in the dashboard.
